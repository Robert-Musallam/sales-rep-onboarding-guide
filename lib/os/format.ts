/**
 * Shared formatting + classname helpers for every module. Canonical home is
 * lib/os; `lib/utils` re-exports these for backward compatibility.
 */
export { cn, fmtMoney, fmtPct, fmtDate } from "@/lib/utils";
